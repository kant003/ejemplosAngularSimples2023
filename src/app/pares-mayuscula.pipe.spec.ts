import { ParesMayusculaPipe } from './pares-mayuscula.pipe';

describe('ParesMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new ParesMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
